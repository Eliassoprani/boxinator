For backend api spec visit : "http://localhost:5012/swagger/index.html" 

To launch backend with https: "dotnet watch run --launch-profile https" And "dotnet dev-certs https --trust" so your machine truts the https cert.